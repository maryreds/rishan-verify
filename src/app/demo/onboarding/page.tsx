import DemoOnboardingClient from "./demo-onboarding-client";

export const metadata = {
  title: "Vouch — Candidate Onboarding Demo",
};

export default function DemoOnboardingPage() {
  return <DemoOnboardingClient />;
}
